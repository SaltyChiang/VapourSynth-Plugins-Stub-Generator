{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "vspreview",
            "type": "shell",
            "command": "path\\to\\your\\VapourSynth\\python.exe",   //Change to path to the python.exe which your python is using.
            "args": [
                "-m",
                "vspreview",
                "${file}",
            ]
        },
        {
            "label": "check",
            "type": "shell",
            "command": "path\\to\\your\\VapourSynth\\python.exe",   //Change to path to the python.exe which your python is using.
            "args": [
                "${file}"
            ]
        }
    ]
}
